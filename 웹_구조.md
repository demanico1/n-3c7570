# 웹_구조.md — 폰에서 보는 목록이 어떻게 만들어지고 도는가

> 마지막 손질: **2026-09-21** (2차)
> 주소: `https://demanico1.github.io/n-3c7570/` · 비밀번호 `1214`
> 되돌릴 지점: 깃 태그 **`v1`**(1차) · **`v2`**(2차) — 아래 8장

코드만 남으면 "왜 이렇게 돼 있나"를 알 수 없어서 적어 둔다.
**고치기 전에 7장(함정)을 먼저 읽어라.** 전부 실제로 한 번씩 깨진 것들이다.

---

## 1. 전체 그림

```
┌ PC ────────────────────────────────────────────────┐
│ gunmart_20260917.csv      2,263건 원본 (읽기 전용)   │
│ 군마트_가격비교_….csv       분류 (읽기 전용)          │
│ 군마트_쿠팡가.db            손으로 넣은 값            │
│ gunmart_img_*.zip         사진 2,259장               │
│ 파트너스링크.json           쿠팡 딥링크 2,263개        │
│          ↓  웹페이지_만들기.py + 웹틀.html            │
│ docs/  index.html · data.bin · pics.bin · big/*.bin │
└──────────────────┬─────────────────────────────────┘
                   │ git push (site 저장소)
┌ 깃허브 페이지 ────▼─────────────────────────────────┐
│ 정적 파일만. 서버 코드 없음. 비밀번호도 서버가 아니라 │
│ 브라우저가 검사한다                                   │
└──────────────────┬─────────────────────────────────┘
                   │
┌ 폰 ──────────────▼─────────────────────────────────┐
│ 비밀번호 → data.bin 풂 → 목록 → pics.bin 뒤따라      │
│ 찜하기·가격입력은 localStorage (이 브라우저에만)      │
└─────────────────────────────────────────────────────┘
```

**서버가 없다**는 것이 모든 설계의 전제다. 그래서
자료를 잠그고(비밀번호), 사람이 넣은 값은 브라우저에 두고,
옮길 때는 링크에 실어 보낸다.

---

## 2. 파일

| 파일 | 줄수 | 하는 일 |
|---|---:|---|
| `웹틀.html` | 1,109 | 화면 전부. **자료는 한 글자도 없다** |
| `웹페이지_만들기.py` | 281 | 굽는 쪽. CSV·DB·사진을 모아 잠근다 |
| `파트너스링크.json` | — | 상품코드 → 쿠팡 딥링크. 한 번 만들면 안 만든다 |
| `docs/index.html` | — | `웹틀.html` 을 그대로 복사한 것 |
| `docs/data.bin` | 972KB | 글자(상품·가격·분류·링크). **잠김** |
| `docs/pics.bin` | 6.9MB | 목록용 사진 120px 2,259장. **잠김** |
| `docs/big/<코드>.bin` | 26MB | 크게 볼 때 쓰는 원본. 한 장씩 **잠김** |
| `docs/robots.txt` | — | 검색엔진 차단 |

굽는 명령은 이것뿐이다.

```bash
python "웹페이지_만들기.py" 1214 --전체
python "웹페이지_만들기.py" 1214 --전체 --링크   # 파트너스 링크를 새로 만들 때만
```

`--전체` 를 빼면 값이 있는 것만 실린다. `--사진없이` 는 사진을 뺀다.

---

## 3. 잠그는 방법

**깃허브 페이지는 저장소가 공개다.** 화면에만 비밀번호 창을 띄워 봐야
`data.bin` 주소를 바로 치면 그냥 열린다. 그래서 **자료 자체를 잠근다.**

```
비밀번호 ──PBKDF2-SHA256 25만번──▶ 열쇠(256비트)
                                    │
data.bin   MAGIC(5) + 소금(16) + IV(12) + AES-256-GCM 암호문
pics.bin   MAGIC(5)            + IV(12) + 암호문      ← 소금은 data.bin 것을 씀
big/*.bin  MAGIC(5)            + IV(12) + 암호문
```

지켜야 할 것 셋.

1. **열쇠는 한 번만 만들고 IV 는 덩이마다 다르게.** 같은 열쇠에 같은 IV 를
   두 번 쓰면 GCM 이 통째로 깨진다.
2. **원본(big) 의 IV 는 무작위로 뽑지 마라.** `sha256(소금 + 상품코드 + 그림)`
   으로 만든다. 무작위면 구울 때마다 2,259장이 전부 "바뀐 파일" 이 되어
   깃이 매번 27MB 를 밀어 넣는다.
3. **소금은 이어 쓴다.** `docs/data.bin` 이 있으면 그 머리의 소금을 그대로
   쓴다. 소금이 바뀌면 2번의 IV 가 전부 바뀌어 같은 일이 벌어진다.

> 비밀번호 `1214` 는 네 자리다. 잠긴 파일을 내려받아 0000부터 돌리면
> 금방 풀린다. **주소를 모르는 사람이 우연히 들어오는 것만** 막는 수준이다.

### 제목까지 잠근 이유

처음엔 제목·바닥글·정렬 이름표를 HTML 에 그냥 뒀는데, 그러면 비밀번호
없이도 소스만 보면 `군마트 쿠팡가 비교`, `국군복지단`, `군마트가 싼 순` 이
다 드러난다. 검색엔진도 그 글자를 긁어간다.

지금은 **제목·안내문·바닥글·정렬 이름표·분류 차례까지 전부 잠긴 자료에서
꺼내 온다.** 잠기기 전 화면에 남는 한글은 `비밀번호를 입력하세요` 뿐이다.

```html
<title>Notes</title>          ← 잠긴 뒤에 진짜 제목으로 바뀐다
```

---

## 4. 받아 오는 차례

```
1) index.html          55KB   화면과 셈법. 자료 없음
2) data.bin           972KB   비밀번호로 풀면 목록이 뜬다 (0.1초)
3) pics.bin           6.9MB   뒤에서 조용히 받아 사진을 채운다
4) big/<코드>.bin       12KB   사진을 눌렀을 때만 그 한 장
```

한 덩이로 묶었더니 11MB 가 되어 폰에서 흰 화면만 오래 봤다. 그래서 나눴다.
`loadPics()` 는 실패해도 목록이 멀쩡하도록 `try` 로 감싸 둔다.

---

## 5. 화면 쪽 (`웹틀.html`)

### 5.1 자료 한 줄의 모양

```js
{ no, p,              // 순번, 상품코드
  name, maker, spec,  // copy_text() 로 다듬은 이름 · 업체 · 규격
  big, mid, sub,      // 대·중·소분류
  g,                  // 군마트 판매가
  c, q,               // 쿠팡 낱개가, 수량
  rate, diff,         // 할인율(%), 차액(원)  ← 군마트가 싸면 음수
  conv, nm,           // 환산 근거, 비교군 없음
  a,                  // 파트너스 딥링크
  hb, hx }            // 찾기용 본문 / 곁들이
```

**부호 규칙** — 기준이 쿠팡가다. 군마트가 싸면 `rate` 와 `diff` 가 **음수**.
프로그램의 `Model.calc()` 과 같다. 바꾸면 양쪽이 어긋난다.

### 5.2 찾기

세 가지가 겹쳐 있다.

**(1) 두 글자씩 끊어 견준다.** 한글은 붙여 쓰기 때문에 낱말로 맞추면 안 맞는다.

```
'참' 만 쳐도 · '참기' 까지 쳐도 · '참ㄱ'(조합 중)도 걸린다
```

`toJamo()` 가 한글을 자모로 풀어 이어 붙인다. `참ㄱ` → `ㅊㅏㅁㄱ` 이
`참기름` 의 `ㅊㅏㅁㄱㅣㄹㅡㅁ` 에 들어 있으므로 걸린다.

**(2) 초성 검색.** `ㅊㄱㄹ` → 참기름. 친 글자가 전부 자음이면 초성으로 본다.

**(3) 본문과 곁들이를 나눠 줄을 세운다.** 이게 핵심이다.

```
hb (본문)   이름 · 업체 · 규격        맞으면 2점
hx (곁들이) 상품코드 · 다른 말        맞으면 1점
```

`오일` 로 찾으면 중분류가 `오일/소스/양념` 인 것이 98건 딸려 와 진짜 기름이
파묻혔다. 그래서 **분류 글자는 찾기에서 아예 뺐고**, 본문에 맞은 것을
위로 올린다. 125건 → 27건이 됐다.

**(4) 다른 말.** `alias_words()`(프로그램 쪽)가 만든다.

```
오일 = 기름 = 식용유      올리브유 → '올리브오일' 로도 찾힌다
우유 = 밀크   계란 = 달걀   화장지 = 휴지 = 티슈   막걸리 = 탁주
```

두루마리 화장지는 이름에 `휴지` 도 `화장지` 도 없고 **소분류에만** 있다.
그래서 별칭은 분류도 보고 만들되, `_CAT_ALIAS` 에 적힌 몇 개만 끌어온다.
분류 이름을 통째로 넣으면 (3)에서 없앤 문제가 되살아난다.

**(5) 띄어쓰기 무시.** 자모로 풀 때 공백을 지운다. `올리브오일` 로 쳐도
`올리브 오일` 이 걸린다.

### 5.3 보기

| 값 | 무엇 |
|---|---|
| `all` | 전부 |
| `done` | 값 있는 것만 |
| `wait` | 아직 안 본 것 |
| `none` | 비교군 없음만 |
| `cart` | 찜한 것 |
| `mine` | 폰에서 넣은 것 |

**찜한 것은 따로 만든 창이 아니라 목록의 한 보기다.** 그래야 사진도 값도
할인율도 목록에서 보던 그대로 나오고 검색·정렬이 그대로 듣는다.

> 한때 찜한 것 보기에만 사진 아래 파란 `쿠팡이동` 단추를 뒀다. 그 뒤에 카드
> 오른쪽 위에 화살표를 넣으면서 같은 일을 하는 길이 셋이 되어 걷어냈다.
> **길을 새로 낼 때는 먼저 있던 길을 지울지 같이 정해라.**

### 5.4 사람이 넣는 값

전부 `localStorage` 다. **주소마다 따로** 쌓인다.

| 열쇠 | 담는 것 |
|---|---|
| `gmpx-key` | 비밀번호 (기억하기를 켰을 때) |
| `gmpx-cart` | 찜한 상품코드 목록 `["32814", …]` |
| `gmpx-mine` | 넣은 값 `{"32866":{"c":14540,"q":2}, "34134":{"nm":1}}` |
| `gmpx-state` | 쿠팡 다녀올 때 보던 자리 (sessionStorage) |
| `gmpx-theme` | 밝게/어둡게 |

`applyMine()` 이 넣은 값을 줄에 덮어씌운다. 원래 값은 `c0·r0·d0·q0·n0` 에
따로 둬서 지우면 되돌아간다. 셈은 프로그램의 `calc()` 과 같다.

```
낱개 = 금액 ÷ 수량        차액 = 군마트가 - 낱개        할인율 = 차액 ÷ 낱개
```

### 5.5 프로그램으로 옮기기

`mineText()` 가 **`여러 건 넣기`(Ctrl+B)가 그대로 알아듣는 글**을 만든다.

```
32866, 14540, 2
33056, 8500, 2
34134, 없음
```

`폰에서 넣은 것` 보기 → `프로그램에 넣을 글 복사` → PC 로 옮겨 붙여넣기.
실제로 `parse_bulk()` 에 돌려 세 줄 모두 제대로 읽는 것을 확인했다.

### 5.6 다른 주소로 옮기기 (백업)

주소가 바뀌면 저장한 것이 따라오지 않는다. 브라우저 규칙이라 우회할 수 없다.
대신 **짐을 주소 뒤에 실어 보낸다.**

```
https://…/#import=eyJ2IjoxLCJjIjpbIjMyODE0Il0sIm0iOnt9fQ
                  └ {v:1, c:[찜한 코드], m:{넣은 값}} 을 base64 로 ┘
```

`#` 뒤는 **서버로 안 간다.** 브라우저가 들고 있다가 페이지에만 준다.
그래서 깃허브도 무엇을 찜했는지 모른다.

- `⋯ → 옮길 링크 복사` → 새 주소에서 열면 풀어 넣는다
- 두 번 열어도 겹치지 않는다
- 주소창에 붙여 넣으면 `#` 뒤만 바뀌어 페이지가 다시 안 열리므로
  `hashchange` 도 같이 지켜본다

브라우저를 갈아타거나 사이트 데이터를 지웠을 때 되살리는 백업으로도 쓴다.

### 5.7 쿠팡으로 가기

```
🔴 쿠팡 가격 확인하기   →  파트너스 딥링크 · 같은 창
```

- **같은 창**이다. 새 창도 해 봤지만 사장님이 같은 창을 골랐다.
  브라우저에 머물면 뒤로가기 한 번으로 보던 자리에 돌아온다.
- 폰에서는 안드로이드가 링크를 가로채 **쿠팡 앱**을 띄운다. 그러면 그 창을
  앱이 가져가므로 뒤로는 못 온다. 대신 `gmpx-state` 에 적어 둔 자리로
  브라우저를 다시 열면 되살아난다.
- 딥링크는 **구울 때 PC 에서** 만든다. 저장소가 공개라 키를 페이지에
  넣으면 그대로 새어 나간다. 화면에는 이미 만들어진 주소만 있다.

---

## 6. 분류 (프로그램과 공유)

`gunmart_price_entry.py` 의 `classify()` 하나를 웹도 그대로 쓴다.
받아 온 분류표는 대분류가 22가지에 136건이 비어 있고 엉뚱하게 박힌 것도
많아서 **여섯 칸으로 다시 짰다.**

```
식품 1,268 · 화장품 225 · 주류 86 · 생활용품 298 · 의류/잡화 354 · 기타 32
```

가리는 차례가 중요하다. **위가 아래를 이긴다.**

1. `is_liquor()` → 주류
2. 담배 회사(KT&G·필립모리스) → 기타
3. 뜻이 하나뿐인 낱말(`_STRONG`) — 치킨·만두·치약·세제
4. **업체 이름**(`_BY_MAKER`) — 옷만 만드는 회사, 먹는 것만 만드는 회사
5. 중분류 → 소분류 (`_MID_TO_BIG`, `_SUB_TO_BIG`)
6. 이름 열쇳말(`_BY_NAME`)
7. 나머지 → 기타

4번이 5번보다 위인 이유는 분류표가 이 지경이라서다.

```
댑코리아 80D스타킹     분류표는 화장품 쪽
쌍방울 내의            중분류가 '엔진오일/첨가제/필터'
급냉삼겹살             중분류가 '충전용품'
```

자세한 것과 주류 가리는 법은 `CLAUDE.md` 4.18-1 에 있다.

---

## 7. 함정 — 고치기 전에 읽어라

### 7.1 `<i>` 는 기울임이다

아이콘 칸을 전부 `<i>` 로 만들었는데 `<i>` 의 기본값이 italic 이다.
★ · ✕ · ₩ 가 차례로 기울어 보였고 **세 번 같은 데서 났다.**
`i{font-style:normal}` 한 줄로 막았다. 그림(SVG)은 글자가 아니라 멀쩡해서
오히려 원인을 가렸다.

### 7.2 덮개를 닫는 것과 자리를 옮기는 것은 다르다

`closeSheet()` 는 `history.back()` 이다. 쿠팡으로 보내라고 시켜 놓고 바로
이어서 부르면 **방금 시킨 이동이 취소된다.** 아무 일도 안 일어난 것처럼
보인다. 실제로 그래서 "쿠팡 이동이 안 된다" 는 말을 들었다.

덮개(확대·고르기)는 **감추기**(`hideOverlays`)와 **뒤로가기**(`dismiss`)를
갈라 써야 한다.

### 7.3 스크롤은 우리가 되돌린다

`history.scrollRestoration` 을 `manual` 로 꺼야 한다. 켜져 있으면 뒤로가기
때 브라우저가 제 기록대로 스크롤을 다시 맞춰서 우리가 옮겨 놓은 자리를
0 으로 덮는다.

### 7.4 값을 넣으면 그 줄이 사라진다

`아직 안 본 것` 으로 걸러 놓고 값을 넣으면 목록을 다시 거르는 순간 그 줄이
조건에서 벗어나 눈앞에서 사라진다. 얼마나 싼지 볼 새가 없다.

그래서 `refreshCard(p)` 가 **거르지도 줄 세우지도 않고 그 칸만 갈아 끼운다.**
요약 세 칸은 `stats()` 로 따로 떼어 숫자만 고친다.

### 7.5 상품명을 누르면 클립보드가 덮인다

프로그램 쪽 이야기다. `여러 건 넣기` 에 붙여 넣으려고 클립보드를 채워 둔 뒤
표에서 상품명을 누르면 `copy_text()` 가 클립보드를 가져간다.
**클립보드는 창을 연 다음에 채워라.**

### 7.6 쿠팡을 틀(iframe) 안에 띄울 수 없다

쿠팡이 iframe 자체를 막지는 않는다. 틀은 뜬다. 그런데 **쿠키가 안 따라
들어가서** 그 안에는 `사용권한이 없습니다` 가 뜬다. 홈을 먼저 거쳐도
마찬가지였다(실제로 해 봤다).

### 7.7 `intent://` 는 쿠팡 앱이 안 받는다

앱이 없을 때 대체 주소로 보내려고 `intent://…;package=com.coupang.mobile;
S.browser_fallback_url=…` 을 써 봤는데, **앱이 깔려 있는데도** 안내 화면으로
떨어졌다. 쿠팡 앱이 그 모양의 주소를 안 받는다. 되돌렸다.

평범한 `https://link.coupang.com/a/…` 로 보내면 안드로이드가 알아서 앱을
띄운다. **그대로 두는 게 맞다.**

### 7.8 '사용권한이 없습니다' 는 원인이 둘이다

| 언제 | 진짜 원인 | 홈 먼저 거치면 |
|---|---|---|
| 첫 요청부터 | **쿠키가 없음** | ✅ 풀림 |
| 60~76번째쯤 | **너무 많이·빨리** | ❌ 소용없음 |

프로그램은 ①을 이미 막아 두었다(`Chrome._work()` 가 홈을 먼저 연다).
겪은 건 ②다. 막힌 뒤 홈을 열어 보면 정상으로 뜨는데 검색만 막혀 있다.
**하루면 풀린다.** 우회하지 마라 — `CLAUDE.md` 4.19.

### 7.9 앱 유무를 묻는 장치는 만들었다가 걷어냈다

쓸모 있는 경우가 '앱도 없고 그 브라우저로 쿠팡이 처음' 인 사람뿐인데,
그러자고 **모두에게 탭을 한 번 더** 시켰다. 드문 경우를 막자고 늘 쓰는 길을
불편하게 만들면 안 된다. 안내(`⋯ → 쿠팡이 안 열릴 때`)만 남겼다.

---

## 8. 되돌리기

### 8.0 돌아갈 수 있는 자리

| 태그 | 언제 | 무엇 |
|---|---|---|
| `v1` | 2026-09-20 | 폰에서 보는 목록 1차 완성 |
| `v2` | 2026-09-21 | 값 넣는 흐름을 다듬음 (무엇이 달라졌는지는 `백업/웹2차_20260921/되돌리는_법.md`) |

### 8.1 올라간 것 되돌리기

```bash
cd <site 저장소>
git log --oneline v2 -1          # 어디인지 본다
git checkout v2 -- .             # 그 시점 파일로 되돌린다
git commit -m "2차 백업본으로 되돌림"
git push
```

`v1` 로 더 뒤로 갈 수도 있다. 다만 값 넣는 흐름이 다시 불편해진다.

### 8.2 굽는 재료 되돌리기

`백업/웹2차_20260921/` (또는 `웹1차_20260920/`) 에 `웹틀.html` 과
`웹페이지_만들기.py` 가 있다. 이 둘만 제자리에 덮으면 나머지는 다시 구우면 된다.

```bash
copy "백업\웹2차_20260921\웹틀.html" .
copy "백업\웹2차_20260921\웹페이지_만들기.py" .
python "웹페이지_만들기.py" 1214 --전체
```

`docs/big/` 은 소금이 같으면 한 장도 다시 안 쓴다.

### 8.3 폰에 쌓인 것 되돌리기

페이지 쪽 일이라 코드를 되돌려도 안 사라진다. 필요하면
`⋯ → 옮길 링크 복사` 로 먼저 받아 두고, 나중에 그 링크로 되살린다.

---

## 9. 다음에 손댈 때

- **값 하나를 고치면 양쪽을 봐라.** 셈(`calc`)·분류(`classify`)·이름
  다듬기(`copy_text`)·찾기용 글자(`body`/`side`)는 프로그램과 웹이 같은
  것을 쓴다. 한쪽만 고치면 어긋난다.
- **굽고 나서 반드시 폰 크기로 확인해라.** 데스크톱에서만 보면 카드가
  겹치는 것을 못 본다.
- **올린 뒤에는 깃허브 페이지가 반영될 때까지 30초쯤 걸린다.** 바로
  확인하면 옛것이 보인다.
